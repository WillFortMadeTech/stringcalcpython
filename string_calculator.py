def add(numbers: str) -> bool:
    """Placeholder for the String Calculator kata.

    Returns True so the very first test can pass. Replace the body as
    each kata step drives out real behaviour.
    """
    return True
